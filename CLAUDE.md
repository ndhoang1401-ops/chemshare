@AGENTS.md

# Dự án: Nguyên Tố

Trước khi code bất kỳ giai đoạn nào, đọc:

- `ROADMAP.md` — lộ trình 13 giai đoạn, giai đoạn nào đã hoàn thành
- `NEXTJS_NOTES.md` — breaking changes Next.js 16 bắt buộc tuân thủ
  (proxy.ts thay vì middleware.ts, async params, v.v.)
- `README.md` của thư mục trống tương ứng (mỗi thư mục chưa có code đều có
  `README.md` ghi rõ giai đoạn nào sẽ lấp đầy nó)

Build tiếp trên code đã có, không viết lại từ đầu. Sau khi hoàn thành một
giai đoạn, cập nhật lại trạng thái trong `ROADMAP.md`.
